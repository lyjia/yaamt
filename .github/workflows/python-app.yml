# This workflow will install Python dependencies, run tests and lint with a single version of Python
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-python

name: Python application

on:
  push:
    branches: [ "master", "development" ]
    tags:
      - '[0-9]+.[0-9]+.[0-9]+'
  pull_request:
    branches: [ "master", "development" ]

permissions:
  contents: read


jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 7
    steps:
    - uses: actions/checkout@v4
    - name: Set up Python 3.12
      uses: actions/setup-python@v5
      with:
        python-version: "3.12"
    - uses: ./.github/actions/setup-deps
    - name: Install test dependencies
      run: |
        pip install flake8 pytest
    - name: Lint with flake8
      run: |
        # stop the build if there are Python syntax errors or undefined names
        flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
        # exit-zero treats all errors as warnings. The GitHub editor is 127 chars wide
        flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
#    - name: Test with pytest (in xvfb)
#      run: |
#        xvfb-run -a pytest
    - name: Test with pytest (no xvfb)
      run: |
        pytest

  # DISABLED: Build job temporarily disabled to conserve GitHub Actions minutes.
  # To re-enable, uncomment this entire build job section.
  # Uses PyInstaller via build.py with unified yaamt.spec.
  #build:
  #  needs: test
  #  strategy:
  #    matrix:
  #      include:
  #        - os: ubuntu-latest
  #          arch: x64
  #        - os: windows-latest
  #          arch: x64
  #        - os: macos-latest
  #          arch: x64
  #        - os: macos-14
  #          arch: arm64
  #  runs-on: ${{ matrix.os }}
  #
  #  steps:
  #  - uses: actions/checkout@v4
  #
  #  - name: Set up Python 3.12
  #    uses: actions/setup-python@v5
  #    with:
  #      python-version: '3.12'
  #
  #  - uses: ./.github/actions/setup-deps
  #
  #  - name: Install Inno Setup (Windows only)
  #    if: runner.os == 'Windows'
  #    run: choco install innosetup -y
  #
  #  - name: Determine version name
  #    id: version_info
  #    shell: bash
  #    run: |
  #      ref_name="${{ github.ref_name }}"
  #      ref_name_sanitized="${ref_name//\//-}"
  #      if [[ "${{ startsWith(github.ref, 'refs/tags/') }}" == "true" ]]; then
  #        VERSION_NAME="$ref_name_sanitized"
  #      else
  #        VERSION_NAME="$ref_name_sanitized-${{ github.sha }}"
  #      fi
  #      echo "VERSION_NAME=$VERSION_NAME" >> $GITHUB_OUTPUT
  #
  #  - name: Build, archive, and create installer
  #    shell: bash
  #    run: |
  #      BUILD_ARGS="--release --archive --version-name ${{ steps.version_info.outputs.VERSION_NAME }} --archive-platform ${{ matrix.os }} --archive-arch ${{ matrix.arch }}"
  #      if [[ "${{ runner.os }}" == "Windows" ]]; then
  #        BUILD_ARGS="$BUILD_ARGS --installer"
  #      fi
  #      python build.py $BUILD_ARGS
  #
  #  - name: Determine archive name
  #    id: archive_info
  #    shell: bash
  #    run: |
  #      if [[ "${{ matrix.os }}" == "windows-latest" ]]; then
  #        ARCHIVE_NAME="yaamt-${{ steps.version_info.outputs.VERSION_NAME }}-${{ matrix.os }}-${{ matrix.arch }}.zip"
  #        echo "ASSET_CONTENT_TYPE=application/zip" >> $GITHUB_OUTPUT
  #      else
  #        ARCHIVE_NAME="yaamt-${{ steps.version_info.outputs.VERSION_NAME }}-${{ matrix.os }}-${{ matrix.arch }}.tar.gz"
  #        echo "ASSET_CONTENT_TYPE=application/gzip" >> $GITHUB_OUTPUT
  #      fi
  #      echo "ARCHIVE_NAME=$ARCHIVE_NAME" >> $GITHUB_OUTPUT
  #
  #  - name: Upload archive artifact
  #    if: github.ref == 'refs/heads/master' || github.ref == 'refs/heads/development'
  #    uses: actions/upload-artifact@v4
  #    with:
  #      name: ${{ steps.archive_info.outputs.ARCHIVE_NAME }}
  #      path: ${{ steps.archive_info.outputs.ARCHIVE_NAME }}
  #      retention-days: 7
  #      compression-level: 9
  #
  #  - name: Upload installer artifact (Windows)
  #    if: runner.os == 'Windows' && (github.ref == 'refs/heads/master' || github.ref == 'refs/heads/development')
  #    uses: actions/upload-artifact@v4
  #    with:
  #      name: yaamt-${{ steps.version_info.outputs.VERSION_NAME }}-windows-${{ matrix.arch }}-setup.exe
  #      path: build/release-*/yaamt-*-setup.exe
  #      retention-days: 7
